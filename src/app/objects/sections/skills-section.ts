import {Section} from './section';
import {Constants} from '../constants';

export class SkillsSection extends Section {

    constructor() {
        super(Constants.SECTION.SKILLS, -100, -100);
    }

}
